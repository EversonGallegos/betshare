import React from 'react'
import { AuthButton } from './styles/header.styles'

const LogoutButton = () => {
    return (
        <AuthButton>
            Logout
        </AuthButton>
    )
}

export default LogoutButton
